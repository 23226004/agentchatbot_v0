# 실 backend 연동 주소. 비우면(또는 미설정) mock 모드로 동작한다.
# backend(FastAPI) 기본 포트 예시 — 실제 포트에 맞춰 조정.
# VITE_API_BASE=http://localhost:8000

# 참고: backend CORS 기본 허용 origin 은 http://localhost:5180 (이 FE dev 포트).
# 다른 포트로 띄우면 backend 의 CORS_ORIGINS 에 그 origin 을 추가해야 한다.
