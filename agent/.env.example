# OpenAI 호환 LLM 서버 (vLLM/TGI 등)
# 서버는 8000 또는 8080 포트에서 제공됨 — 경로 끝에 /v1 포함
LLM_BASE_URL=http://localhost:8000/v1
# LLM_BASE_URL=http://localhost:8080/v1

# 서버에 로드된 모델 이름 (필수)
LLM_MODEL=

# 자체 서버는 보통 더미값 사용
LLM_API_KEY=EMPTY

# 선택값
LLM_TEMPERATURE=0.0
LLM_TIMEOUT=60
